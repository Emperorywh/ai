{
  "name": "coding-agent-workflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "通过需求访谈、规格拆解和独立 Claude Code 会话顺序实现任务的最小工作流。",
  "files": [
    "dist"
  ],
  "bin": {
    "caw": "./dist/cli/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint --ext .ts --no-error-on-unmatched-pattern src test",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.206",
    "@anthropic-ai/sdk": "^0.110.0",
    "commander": "^12.1.0",
    "yaml": "^2.6.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.16.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "typescript": "^5.5.4",
    "vitest": "^4.1.10"
  }
}
