---
description: 基于需求做架构理解与规格设计，生成 docs/SPEC_xxxx.md
---

请先阅读 AGENTS.md 和项目结构，理解当前架构、数据流、状态流和模块边界。

我要实现一个新需求：$ARGUMENTS

请只做需求分析和规格设计，不要制定开发计划，不要实施代码。

文件名约定：docs/SPEC_xxxx.md 中的 xxxx 替换为本次需求的有意义标识（如功能名或日期），不要保留字面 xxxx。

流程要求：

1. 先梳理当前架构、核心模块、数据入口、状态归属和跨层依赖。
2. 对需求进行访谈澄清。优先使用可用的提问工具；如果当前环境没有提问工具，就直接列出必须回答的问题并等待我确认。
3. 不要询问显而易见的问题。重点追问技术实现、UI/UX、状态流、边界情况、验收标准、风险和取舍。
4. 对于可以合理默认的细节，直接写入“明确假设”，不要把所有细节都丢给用户。
5. 如果当前架构不适合承载该需求，必须先指出需要优先重构的原因，不要把新逻辑堆到不合理结构上。
6. 所有未确认但会影响实现范围的内容，必须进入“待确认问题”，不能被 AI 自行脑补。
7. 问题确认后，生成 docs/SPEC_xxxx.md。

SPEC 必须包含：

- 背景与目标
- 非目标范围
- 当前架构理解
- 关键问题与结论
- 明确假设
- 待确认问题
- 用户场景
- UI/UX 行为
- 数据结构
- 数据流
- 状态流
- 模块边界
- 边界情况
- 验收标准
- 测试策略
- 风险与取舍
- 用户确认记录

暂时不要生成 PLAN，不要实施代码。
