# <功能名> — 技术规格说明（SPEC）

> 阶段 1（深度访谈）产出。**该功能的单一事实源**，下游 ROADMAP / PROGRESS 都读它。
> 禁止「后续考虑 / 可以扩展 / 视情况而定」——出现就是访谈没做完。

| 项 | 值 |
|---|---|
| 文档版本 | v1.0 |
| 日期 | {{YYYY-MM-DD}} |
| 所属功能 | `<功能名>`（`docs/features/<功能名>/`） |
| 状态 | 已定稿（基于 N 轮访谈） |

---

## 0. 访谈决策汇总（Decision Log）

> 访谈过程中每个**需要拍板**的议题都记一行。这是 SPEC 最有价值的部分——它固化了「为什么这么定」，下游不再反复纠结。带默认值的决策标 **【默认】**。

| # | 议题 | 决策 | 理由 / 权衡 |
|---|---|---|---|
| D1 | （例：技术栈/框架选型） | （明确选什么） | （为什么、放弃了什么） |
| D2 | （例：数据来源/外部依赖） | | |
| D3 | （例：MVP 范围） | | |

---

## 1. 现状（增量开发/接手既有代码时必填；全新功能删本节）

- 现有架构 / 技术栈 / 关键模块：
- 本功能与现有代码的关系（复用 / 扩展 / 新增）：

## 2. 功能概述与目标

- 一句话目标：
- 用户价值（做完后用户能做什么）：

## 3. 非目标范围（明确不做什么）

-

## 4. 用户角色与场景

- 角色：
- 核心场景（谁、在什么情况下、要达成什么）：

## 5. 功能需求

- （逐条可验收的 needs）

## 6. 非功能需求（性能 / 安全 / 可用性 / 兼容性）

-

## 7. 技术栈

| 层 | 选型 | 说明 |
|---|---|---|

## 8. 架构与关键设计（粗粒度，细节留给 build）

- 目录结构 / 模块职责 / 数据流（够 downstream 拆 Task 即可，不过早冻结）：

## 9. 数据模型（粗粒度）

## 10. API / 接口设计（粗粒度）

## 11. 边界情况与陷阱清单

> 访谈里挖出来的「容易出错、容易返工」的点，逐条给应对。**这是访谈深度的体现。**

| 陷阱 | 说明 | 应对 |
|---|---|---|

## 12. 风险与权衡（开放问题）

> 标注每项的**推荐默认值**，无异议即按默认推进。

1. （例：A 方案省事但限制未来 / B 方案更稳但工期长 ——【默认】选 A，理由…）
2.

## 13. MVP 范围与阶段里程碑（粗线条）

> 给 ROADMAP 一个起点；精确的 Milestone/Task 拆分在阶段 2。

- Phase 1（MVP）：做什么、验收点
- Phase 2+：增量项

## 14. 待确认（带默认值，不阻塞开发）

- [ ] （例：XXX 接受 【默认值】）—— 默认接受
- [ ]

---

> 本 SPEC 为访谈定稿。重大变更需更新本文档版本与日期，并同步 ROADMAP/PROGRESS。
